class Solution {
 public:
  bool canJump(vector<int>& nums) {
    int i = 0;

    for (int maxi = 0; i < nums.size() && i <= maxi; ++i)
      maxi = max(maxi, i + nums[i]);

    return i == nums.size();
  }
};
